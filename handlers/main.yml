---
- name: restart ansible ara stack
  ansible.builtin.command: >-
    docker compose
    --project-name {{ ara_compose_project_name }}
    -f {{ ara_compose_dir }}/docker-compose.yml
    up -d --force-recreate --remove-orphans
  changed_when: false
